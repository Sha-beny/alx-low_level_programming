#include "main.h"
#include <unistd.h>
/**
 *
 * _putchar.c - writes the character c to stdout 
 * 0c: The character to point
 *
 * Return: on success 1.
 * on error, -1 is returned, and errno is set appropriately.
 */
int _putchar.c(char c)
{
	return (write(1, &c, 1));
}
